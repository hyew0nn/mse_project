server {
    listen 80;

    # 프론트엔드
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # API 요청은 8080으로 프록시
    location /api {
        proxy_pass http://app:8080;
    }
}